The main objective is to create a header file for implementing binary tree and then execute its basic operations.
By creating a header, we enhance code reusabilty and save time.

Just make sure that you download both the header file and the cpp file (they have to be in the same directory).
